interface:
  display_name: "Agentic RAG"
  short_description: "Plan, route, retrieve, check context sufficiency, iterate, and synthesize grounded RAG answers."
  default_prompt: "Use the agentic-rag skill to design or implement a multi-hop, multi-source RAG pipeline with Sufficient Context checks."
policy:
  allow_implicit_invocation: true
